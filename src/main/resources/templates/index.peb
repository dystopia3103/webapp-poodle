    {% extends "base" %}

    {% block main %}
<ul>
    {% for event in events %}
        <li>
            {{ event.name }} | {{ event.description }} | {{ event.link }}
        </li>
    {% endfor %}
</ul>
{% endblock %}